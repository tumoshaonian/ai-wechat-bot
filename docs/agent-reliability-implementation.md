# 稳定性实施记录

更新：2026-09-06。这是阶段性交付，不代表完整计划已经完成。

## Harness 更新核对

- 比较范围：`0a53fb55be..d347e70390`，后者是 0.1.3-alpha.1 发布合并。
- 本次更新没有修改 Python `api.py` 和 SDK `server.ts` 的实际调用接口；仍只有初始化、提示和关闭等入口，没有新增 Python 单轮取消或冷会话恢复方法。
- 上游更新涉及 Session 事件/持久化与 compaction 内部实现，不应自行按旧格式重写上游 JSONL 文件。本轮没有修改上游仓库。
- 本机 Python SDK 来自同级源码目录，但实际可执行文件 `deepseek-harness-sdk-runtime-win-x64.exe --version` 返回 `0.1.2-alpha.2`。本轮真实 smoke 测试针对这个实际 Runtime，不代表新版 Runtime 已验证。未替换二进制，也未迁移原有 Harness 历史文件。

## 已实现

- 删除自然语言桌面文件匹配器及其快捷返回分支；普通请求完整进入 Harness；旧 `/文件` 也转为 Agent 请求。
- 添加 loopback 交付服务与 `deliver_file` MCP 工具：随机服务凭据、本轮临时票据、可信接收对象绑定、文件读取/发送权限检查、50MiB 文件上限、任务内及持久化交付去重、失败或未知回执不自动重发。
- 实际交付回执写入模型工具结果和渠道历史；文件标签仅保留兼容路径，不再自动合并中间截图。
- 新增 SQLite 渠道事实日志与逻辑会话 epoch；运行时身份加入随机命名空间，避免重启同名创建冲突。重建本版开始记录的请求、回复和交付事实；普通失败不清空已记录事实；`end` 不继承旧 epoch。
- 会话身份包含连接 ID，并同步修正后台结束会话命令及内存消息去重命名空间。
- 原生 compaction 覆盖配置显式启用，阈值 75%、近期保留比例16%。冷恢复另有字节预算，超过时尝试有界摘要加近期完整轮次；摘要不合格或达到上限则明确报错。
- 渠道摘要保存不可变检查点与覆盖序号，不删除原始事实；按 epoch 和前一检查点做事务校验，避免 `end` 后旧摘要污染新会话。晚到回执继续追加，不被旧检查点吞掉。
- 摘要使用临时隔离的 Harness Profile，关闭桌面工具、禁止全部工具执行。最多 8 批，至少保留最新完整轮次。停止／结束会话覆盖恢复阶段，重复取消不遗留摘要进程清理任务。恢复成功及失败元数据进入现有任务事件，不写完整历史正文。
- `prepare_window` 识别无所有者的主窗口候选，排除小辅助窗口；验证 HWND/PID 后恢复最小化或屏幕外窗口，再确认可见和可交互。候选不唯一时拒绝猜测。
- 豆包问答接入窗口准备；控件缺失不再自动杀进程重启。
- 整轮错误解释和 Runtime teardown 纳入同一串行边界，避免旧任务失败关闭后续任务；取消返回后不继续交付已中断结果。
- 显示实际工具阶段；未解决工具错误或输出上限不标为完全成功；上传未确认时明确部分完成。

## 已执行测试

- 修改前 Python 基线：116 项通过。
- 首批 Python 回归：125 项运行，124 项通过、1 项 opt-in 交互桌面测试跳过；跳过项已另行开启并通过。`git diff --check` 与 PowerShell AST 语法检查通过。
- 摘要恢复阶段 Python 回归：135 项运行，134 项通过、1 项 opt-in 桌面测试跳过；新增覆盖摘要预算、取消、重复取消清理、`end` 隔离、晚到回执和检查点范围校验。
- 独立 WinForms 真实窗口测试：最小化3次、屏幕外3次全部通过，校验返回 PID 与测试进程一致、UIA 非屏幕外；测试进程已清理。
- 真实 Harness + 当前 SDK profile + 新增 MCP 初始化通过。
- 真实模型两轮及重启测试：记住代号“蓝鲸731”；重启后答出代号及测试交付文件；`end` 后回答不知道旧代号。
- 真实 Harness → Desktop MCP → loopback 交付服务 → 测试接收器通过，接收器只收到1次文件。这里没有实际发送企业微信，不能称为微信收件验收。
- 真实豆包单次问答通过：输入回读、`submitted=true`、`submission_verified=true`、回答文本和 HWND 截图均确认，`accessibility_restarted=false`。人工查看截图确认问题及回答可见。该通用测试问题追加在当前豆包对话，窗口保留打开。
- 截图留在被 Git 忽略的 `.harness-sessions/regression-artifacts/doubao-20260906-001940.png`，未提交到仓库。
- 真实摘要 Harness 执行拦截探针通过：直接尝试执行测试工具返回拒绝，执行函数未运行，不仅依赖提示词禁用。
- 真实长历史恢复通过：14 轮历史生成 3 个检查点，原始事实共 44 条完整保留，恢复内容为 5117 UTF-8 字节（预算 8192）；模型准确答出 `D:/sandbox/星桥计划`、最后轮次13和旧交付 `unknown`。这是渠道摘要测试，不代表完整原生日志恢复或微信收件验收。

可重复命令（从项目根目录）：

```powershell
$env:PYTHONPATH='wechat-aibot-bridge/src'
.\.venv\Scripts\python.exe -m unittest discover -s wechat-aibot-bridge/tests -q
$env:WECOM_RUN_DESKTOP_SMOKE='1'
.\.venv\Scripts\python.exe -m unittest discover -s wechat-aibot-bridge/tests -p test_windows_restore_smoke.py -v
Remove-Item Env:WECOM_RUN_DESKTOP_SMOKE
.\.venv\Scripts\python.exe wechat-aibot-bridge/tests/smoke_harness_recovery.py
.\.venv\Scripts\python.exe wechat-aibot-bridge/tests/smoke_summary_guard.py
.\.venv\Scripts\python.exe wechat-aibot-bridge/tests/smoke_history_compaction.py
```

模型 smoke 使用现有模型凭据产生真实 API 调用，但使用临时工作目录，不向企业微信发送消息。guard 探针不调用模型。

## 尚未完成／不能算通过

1. **原生完整冷恢复**：渠道摘要恢复已经实现并通过真实测试，但属于有损压缩，不含完整工具轨迹，也不保证记住所有细节。已启用的 Harness 原生 compaction 只处理活跃会话，尚未做强制原生长上下文压缩验证。超大单轮及超限积压仍安全拒绝。
2. **旧版历史迁移**：连接命名空间已修正，旧日志、旧注册表和后台对话记录保留，但旧版内容不会自动导入新渠道日志。首次升级可视为明确的记忆起点。
3. **所有工具的执行级权限**：新增文件交付执行器有强制校验；现有 shell/通用桌面能力的细粒度权限仍不能仅凭提示词宣称安全隔离，需进一步接入 Harness 执行策略。尚不建议向不可信多人开放完整电脑控制。
4. **持久任务恢复与完整状态机**：已有失败隔离、部分完成、进度和取消修复，但“等待用户确认”、跨进程续跑、多任务资源仲裁、微信通道恢复仍未完整实现。现有通道失效策略仍是停止动作并保留事实，不自动继续后台操作。
5. **真实企业微信收件验收**：交付服务的真实模型路径和测试接收器通过，实际企业微信收件及断网回执尚未测试。
6. **豆包生成并导出文档**：移除了错误的快捷方式发送路径；并不意味着已增加豆包云端文档导出能力，该完整场景未验收。
7. **视觉客户端操作**：没有引入新的视觉模型或开放全局键鼠脚本；B站客户端搜索尚未验收，不能以浏览器搜索替代。
8. **管理后台配置界面**：已有任务事件和部分完成状态继续复用；新的上下文用量、恢复方式、交付状态未知展示和可编辑策略页面尚未完整补齐。

## 部署与回退

首批稳定性修改已提交并推送到 `origin/master`，提交号 `9ddbd20`；之后的渠道摘要恢复属于下一批本地修改。尚未重启生产 Bridge/Java，也未修改真实 `.env`。本轮只启动独立测试 Runtime；生产界面不会自动加载 Python 修改，需要安排一次受控重启。

部署前先备份 `.runtime/admin`、`.harness-sessions` 和真实配置，并停止正在执行的任务。由于尚无旧版历史导入，首次新版本运行应提示用户当前记忆从该版本开始累积。新渠道 SQLite 与旧 Harness 文件并存，不需要删除旧历史。

回退应还原本轮业务代码及配置覆盖文件，保留新增 SQLite 作为记录，不覆盖或删除上游历史。真正升级到 0.1.3 Runtime 时使用独立 `DSH_HOME` 验证格式兼容与恢复能力后，再决定历史迁移。
